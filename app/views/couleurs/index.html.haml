%h1 Catalogue

%h2 Couleurs
.actions
  = link_to ' + Ajouter une couleur', new_couleur_path()

%table
  %tr
    %th{width: "20px;"}
    %th Intitulé

  - @couleurs.each do |couleur|
    %tr
      %td= link_to '>', edit_couleur_path(couleur)
      %td= couleur.nom



