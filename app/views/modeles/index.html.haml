%h1 Catalogue

%h2 Modèles
.actions
  = link_to ' + Ajouter un modèle', new_modele_path()

%table
  %tr
    %th{width: "10px"}
    %th{width: "30px"} #
    %th Nom
    %th{width: "80px"} Tailles
    %th{width: "60px"} P.U. H.T.
    %th{width: "40px"} Var.

  - @modeles.each do |modele|
    %tr
      %td= link_to '>', modele_path(modele)
      %td.right= modele.numero
      %td.left= modele.nom
      %td.center= "#{modele.taille_min} -> #{modele.taille_max}"
      %td.right!= prix(modele.prix)
      %td.center= modele.versions.count


