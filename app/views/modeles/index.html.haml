%h1 Catalogue

.actions2
  = link_to ' + Ajouter un modèle', new_modele_path()


- @modeles.each do |modele|
  .blob{onclick: "window.location = $(this).attr('url')", url: modele_path(modele)}
    .left
      .designation= modele.nom
      .ref= modele.numero
    .center
      .prix!= prix(modele.prix)
    .right
      .tailles= modele.liste_taille.compact.join(', ')
      .att= (modele.versions.collect {|v| v.couleurs}.join(', '))
