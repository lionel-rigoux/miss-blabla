setup:
  addons:
    - plan: heroku-postgresql
build:
  docker:
    web: Dockerfile
  config:
    RAKE_ENV: production
release:
  image: web
  command:
    - rake db:migrate
    - rake assets:precompile
